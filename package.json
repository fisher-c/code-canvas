{
  "name": "codepair-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cd backend && PATH=\"$HOME/.local/bin:$PATH\" uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 4000",
    "dev:frontend": "cd client && npm run dev",
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run preview --prefix client -- --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
